server {
  listen      80;
  server_name <%= node['yumserver']['nginx']['servername'] %>;

  access_log /var/log/nginx/<%= node['yumserver']['nginx']['servername'] %>.access.log main;
  location / {
    root <%= node['yumserver']['basepath'] %>;
    autoindex on;
  }
}